namespace Scripts.Systems.StateMachine
{
    // an example of an enum for the statemachine
    public enum GameStates
    {
        // replace as needed
        Startup, MainMenu, Play, Pause, GameOver
    }
}